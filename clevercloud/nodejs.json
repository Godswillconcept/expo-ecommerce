{
  "build": {
    "type": "nodejs",
    "build": {
      "type": "custom",
      "cmd": [
        "npm install",
        "cd backend && npm install",
        "cd ../admin && npm install",
        "cd .. && npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "cd backend && node src/server.js",
    "buildDirectory": "."
  }
}
